;;
;; we can just set complex as a structure with
;; two numeral components. this is the same as,
;; for example, (define (rect len wid) ...), for
;; which the length and width could either be
;; integers, rationals, or reals.
;;


;;
;; we just let `sine` and `cosine` function to accept
;; a number and try to convert them into a real number
;; and then do calculation.
;;
