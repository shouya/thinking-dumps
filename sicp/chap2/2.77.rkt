;; Of course this works. before adding these bindings it's unable to
;; access these functions via the generic interface. So what we do is
;; just register these bindings.
;;
;;   (magnitude z)
;;   (magnitude-rectangle z)
;;   (sqrt (+ (* a a) (* b b)))
;;
